"use client";

// QUIZ FEATURE DISABLED
/*
import { useState, useEffect, useCallback } from "react";
import { useRouter } from "next/navigation";
import supabase from "@/supabase/supabase_client";

interface Question { ... }
interface QuizPageProps { subjectId: string; }

export default function QuizSection({ subjectId }: QuizPageProps) {
  // Full quiz UI: loads questions via /api/quiz/start + /api/quiz/questions,
  // handles SM-2 answer submission via /api/quiz/questions (POST),
  // completes quiz via /api/quiz/complete, shows score in sidebar.
}
*/

export default function QuizSection(_props: { subjectId: string }) {
  return null;
}
